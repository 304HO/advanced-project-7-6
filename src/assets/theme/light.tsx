import { ThemeType } from "../../types/theme";

const theme: ThemeType = {
  id: "light",
  primaryColor: "#3f26c7",
  secondaryColor: "black",
  background: "white",
  color: "gray"
};

export default theme;
